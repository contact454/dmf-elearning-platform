{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "UpdateSkillScoreInput",
    "type": "object",
    "required": [
        "userId",
        "skill",
        "scoreVal"
    ],
    "properties": {
        "userId": {
            "type": "string",
            "format": "uuid"
        },
        "skill": {
            "type": "string",
            "enum": [
                "vocabulary",
                "grammar",
                "reading",
                "listening",
                "speaking",
                "writing"
            ]
        },
        "scoreVal": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Mastery level from 0.0 to 1.0"
        }
    }
}
