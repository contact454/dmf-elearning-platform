{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Skill Score",
    "type": "object",
    "required": [
        "userId",
        "skill",
        "scoreVal"
    ],
    "properties": {
        "userId": {
            "type": "string",
            "format": "uuid"
        },
        "skill": {
            "type": "string",
            "enum": [
                "vocabulary",
                "grammar",
                "reading",
                "listening",
                "speaking",
                "writing"
            ]
        },
        "scoreVal": {
            "type": "number",
            "description": "0.0 to 1.0 mastery level"
        },
        "updatedAt": {
            "type": "string",
            "format": "date-time"
        }
    }
}