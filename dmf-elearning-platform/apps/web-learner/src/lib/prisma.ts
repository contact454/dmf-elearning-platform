// Prisma disabled for Vercel deployment
// Enable after setting DATABASE_URL in Vercel environment

// Type-safe null export that allows prisma usage with null checks
const prisma: any = null;
export default prisma;
